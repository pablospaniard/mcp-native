# React Native demo

This folder will host the first end-to-end example: MCP connection, A2UI resource resolution, native rendering, and action dispatch back to MCP.

Package tests now prove the runtime, validation, renderer-local state, and host-callback seams. This folder remains documentation-only until the project adds a real mobile host, explicit renderer-to-agent transport integration, supported iOS and Android targets, and simulator or device verification.
