# React Native demo

This folder will host the first end-to-end example: MCP connection, A2UI resource resolution, native rendering, and action dispatch back to MCP.

It is intentionally documentation-only until the runtime-to-renderer seam is proven by package tests.
